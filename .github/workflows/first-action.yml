name: First Workflow
on: workflow_dispatch
jobs:          
  first-jobs-Windows:
     name: first job
     runs-on: windows-latest
     steps: 
      - name: Print greeting
        run: echo "Hello World"
      - name: print goodbye
        run: echo "Done- bye!"
    
